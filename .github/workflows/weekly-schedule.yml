name: Daily Prayer Schedule Email

on:
  schedule:
    # Two cron entries so the email arrives at ~7 AM Central year-round.
    # Only the entry matching 7 AM Central actually runs (time-gate check below).
    - cron: '17 12 * * *'   # 12:17 UTC = 7:17 AM CDT (Mar–Nov)
    - cron: '17 13 * * *'   # 13:17 UTC = 7:17 AM CST (Nov–Mar)
  workflow_dispatch:
    inputs:
      send_emails:
        description: 'Send real emails (set to false for preview/testing)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

permissions:
  contents: write
  pages: write
  id-token: write
  issues: write

jobs:
  generate-schedule:
    runs-on: ubuntu-latest

    permissions:
      contents: write  # Required to push changes back to repo
      issues: write    # Required to create issue on failure

    steps:
    - name: Check Central Time gate
      if: github.event_name == 'schedule'
      id: time_gate
      run: |
        CENTRAL_HOUR=$(TZ='America/Chicago' date +%H)
        echo "Central Time hour: $CENTRAL_HOUR"
        if [ "$CENTRAL_HOUR" != "07" ]; then
          echo "Not 7 AM Central (hour=$CENTRAL_HOUR), skipping this run"
          echo "skip=true" >> "$GITHUB_OUTPUT"
        else
          echo "skip=false" >> "$GITHUB_OUTPUT"
        fi

    - name: Checkout repository
      if: steps.time_gate.outputs.skip != 'true'
      uses: actions/checkout@v4

    - name: Set up Python
      if: steps.time_gate.outputs.skip != 'true'
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Run prayer schedule generator
      if: steps.time_gate.outputs.skip != 'true'
      env:
        # For scheduled runs, always send emails.
        # For manual runs, respect the send_emails input (defaults to false).
        EMAIL_ENABLED: ${{ github.event_name == 'schedule' && 'true' || inputs.send_emails || 'false' }}
        SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
        SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
        RECIPIENT_EMAILS: ${{ secrets.RECIPIENT_EMAILS }}
      run: |
        python prayer_schedule_V10_DESKTOP_FIXED.py

    - name: Commit and push generated files
      if: steps.time_gate.outputs.skip != 'true'
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "GitHub Actions Bot"
        git add Prayer_Schedule_Current_Week.html Prayer_Schedule_Current_Week.txt prayer_schedule_log.txt archive/
        git diff --staged --quiet || git commit -m "Auto-generate prayer schedule for $(date +%Y-%m-%d)"
        git push || echo "No changes to push"

    - name: Upload schedule artifacts
      if: steps.time_gate.outputs.skip != 'true'
      uses: actions/upload-artifact@v4
      with:
        name: prayer-schedule-${{ github.run_number }}
        path: |
          Prayer_Schedule_Current_Week.html
          Prayer_Schedule_Current_Week.txt
          prayer_schedule_log.txt
        retention-days: 90
        if-no-files-found: warn

    - name: Create or update issue on failure
      if: failure() && github.event_name == 'schedule' && steps.time_gate.outputs.skip != 'true'
      run: |
        existing=$(gh issue list --label "bug" --state open --search "Prayer schedule generation failed" --json number -q '.[0].number' 2>/dev/null || true)
        if [ -z "$existing" ]; then
          gh issue create \
            --title "Prayer schedule generation failed on $(date +%Y-%m-%d)" \
            --body "The daily prayer schedule workflow failed. Please check the [workflow run](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}) for details." \
            --label "bug"
        else
          gh issue comment "$existing" \
            --body "Still failing as of $(date +%Y-%m-%d). [Workflow run](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})"
        fi
      env:
        GH_TOKEN: ${{ github.token }}

  deploy-pages:
    needs: generate-schedule
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
    - name: Wait for push propagation
      run: sleep 30

    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        ref: main

    - name: Prepare site directory
      run: |
        mkdir _site
        cp index.html _site/
        cp .nojekyll _site/
        cp Prayer_Schedule_Current_Week.html _site/ || true
        cp Prayer_Schedule_Current_Week.txt _site/ || true

    - name: Setup Pages
      uses: actions/configure-pages@v5
      with:
        enablement: true

    - name: Upload Pages artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: '_site'

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
